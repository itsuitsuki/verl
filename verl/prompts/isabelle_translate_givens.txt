Read the PROBLEM and formalize ONLY its given conditions.

Output ONLY a VARS line followed by GIVEN lines, nothing else - no explanations, no markdown (<...> are placeholders to replace):
VARS: <name> <int|real>, <name> <int|real>
GIVEN: <one Python boolean expression>
GIVEN: <one Python boolean expression>

Rules:
- Python syntax only: == for equality, explicit * for products, ** for powers, / real division, // integer division, % modulo, and/or/not for logic.
- Allowed functions: sqrt abs gcd lcm floor ceil factorial comb log ln exp sin cos tan arcsin arccos arctan min max guarantee_pair guarantee_same. Write inverse trig as these named functions (arctan(1) for tan^-1(1), arcsin(x) for sin^-1(x)); never as a power of sin/cos/tan. NOTHING else: a quantity like a_n is a plain variable a_n, never a(n). No sets, lists, tuples, subscripts, comprehensions; introduce plain variables for each quantity instead. gcd and lcm take exactly TWO arguments; for three or more integers, nest them, so the LCM of 1 through 4 is lcm(1, lcm(2, lcm(3, 4))). The mathematical constants are pi (write pi for the circle constant, so a right angle is pi/2) and Euler's number written exp(1); never write a bare e for Euler's number -- e is read as an ordinary variable.
- VARS declares every variable exactly once: int for integer/counting/divisibility quantities, real otherwise. A number sequence's terms (a_1, a_n, ...) and its common difference d or common ratio r are REAL even when the stated terms or sums look like integers -- the difference/ratio is often fractional (three sums 48, 40, 32 force a common difference of -8/3), and declaring the terms int makes the arithmetic/geometric relations contradictory (no integer solution) so every step reads as inconsistent.
- Exactly one GIVEN must define answer, and only by NAMING the asked quantity (answer == total_cost) or by the exact expression the question itself states (answer == 10 * a + b for "compute 10a+b"). NEVER define answer by a formula you derived, and NEVER state its numeric value. Exception -- a quantity defined by a standard operator IS naming it, not deriving: "the smallest number divisible by all integers a through b" is by definition their least common multiple, so write answer == lcm(a, lcm(a+1, ..., lcm(b-1, b))) (the nested fold); "the largest number dividing all of them" is the gcd fold. Do NOT try to express "smallest such" with min or a comprehension (not allowed) -- use the lcm/gcd fold.
- STRUCTURAL-PART / notation questions ("in the expression E, the base / exponent / coefficient / degree / leading term is named X", "let a be the coefficient of x^2"): the name is a LABEL the problem introduces for a piece of NOTATION, and its value follows a reading convention (e.g. -4^3 means -(4^3), so its base is 4 not -4) that you can misread and that is not an arithmetic fact to verify. So do NOT compute or pin that value in a GIVEN. Write ONLY answer == <the asked expression in those names> (for "the base is s, the exponent is t, find s - 2t" write just answer == s - 2 * t); the solution's OWN steps state each name's value (s == 4, t == 3) as their premises. Pinning your own parse (s == -4) as a given contradicts a solution that reads it correctly and poisons every later step.
- Pigeonhole / "guarantee" problems: when the problem asks the MINIMUM number of items to draw/pick to GUARANTEE two fall in the same category (a matching pair; two socks/balls of the same color; two people in the same month), do NOT compute the answer yourself -- NAME it with the pigeonhole function: write answer == guarantee_pair(n_categories) and bind the category count (n_categories == 4 for four colors, == 12 for twelve months). To GUARANTEE k items in one category (k of the same kind), write answer == guarantee_same(n_categories, k). NEVER expand these to answer == n_categories + 1 or n_categories * (k - 1) + 1 -- that is a derived formula (forbidden); guarantee_pair / guarantee_same already carry the certified pigeonhole minimum. Per-category stock counts ("at least 2 of each") are just feasibility and need not appear.
- The QUESTION itself is NEVER a given: apart from the single answer == line above, do not state the asked property, the sought value, or the claim the problem asks to show or verify as a GIVEN fact. A given that already contains what the question asks lets a solution step restate it and be graded correct while assuming the conclusion. ("Show that x^2 > 3", "is n divisible by 7?", "find k with 2k+1 = 9": none of these statements may appear as a GIVEN; only the conditions the problem grants may.)
- Bind every numeric quantity the problem states to a named variable (n_students == 20). Each GIVEN is ONE fact from the PROBLEM STATEMENT only: no solution knowledge, no derived facts.
- When the problem has more than one object of the same kind -- two triangles, two circles, an original figure and a derived one, a before state and an after state, OR the SAME quantity evaluated at DIFFERENT input values (a = 9 when b = 2, then a = ? when b = 3 for a = k/b^2) -- give each object/state its OWN names (a_orig vs a_new, area1 vs area2, or a1/b1 for the first evaluation and a2/b2 for the second) and never let one name stand for two different values. Pinning a single variable to two values (b == 2 AND b == 3) makes the givens contradictory and every step fails. For a variation a = k/b^2, introduce the constant k once and write a1 == k/b1^2, a2 == k/b2^2. (This does NOT apply to a number sequence's indexed terms a_1, a_2, a_3, ...: those are ALREADY distinct variables -- keep them as a_1, a_2, ... and relate them with the arithmetic/geometric rule a_k == a_1 + (k-1)*d; do not rename or duplicate them.)
- Numbers must appear in the problem statement (0, 1, 2 and unit conversions like 60 minutes, percent /100 are allowed). If a condition is not arithmetic (a geometric configuration, a story), state only its quantitative part; never invent relations.
- If the problem states a sequence is arithmetic or geometric, its DEFINITION is a given (not an invented relation): with a fresh real common difference d (or ratio r), add a GIVEN a_k == a_1 + (k-1)*d (arithmetic) or a_k == a_1 * r**(k-1) (geometric) for EVERY term a_k the problem or the asked quantity mentions (e.g. a_1..a_9 when a_1+a_4+a_7, a_2+a_5+a_8, a_3+a_6+a_9 appear), so the terms are linked and later steps can be verified.

EXAMPLE PROBLEM: A box holds 17 red and 29 blue marbles. How many marbles are in the box?
EXAMPLE OUTPUT:
VARS: red int, blue int, answer int
GIVEN: red == 17
GIVEN: blue == 29
GIVEN: answer == red + blue

EXAMPLE PROBLEM: Given that $p = 6.6 \times 10^{-27}$ and $|5x-1| = x+3$, compute $x \cdot \sqrt{p}$.
EXAMPLE OUTPUT:
VARS: p real, x real, answer real
GIVEN: p == 6.6 * 10 ** -27
GIVEN: abs(5 * x - 1) == x + 3
GIVEN: answer == x * sqrt(p)

PROBLEM:
{problem}