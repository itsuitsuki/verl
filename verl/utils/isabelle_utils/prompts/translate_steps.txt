The given conditions of a problem are formalized as (Python syntax):
{vars_givens}

Below are the reasoning steps of a model's solution. Each step lists premises and exactly one conclusion. For EACH step, translate its stated premises AND its conclusion into Python boolean expressions. Output ONLY the STEP lines, one per input step, same numbering, nothing else - no explanations, no markdown:
STEP 1 | premises: <expr> ; <expr> | prop: <expr>
STEP 2 | premises: - | prop: <expr>

Rules:
- premises: the facts THIS step's premise lines state, separated by ' ; ' (write - if it states none). Translate them as written; they must come from the problem or from earlier steps, never from this step's own conclusion.
- prop: the conclusion as ONE Python boolean expression.
- Python subset: == for equality, explicit *, ** powers, / real division, // integer division, % modulo, and/or to combine facts. Allowed functions only (sqrt abs gcd lcm floor ceil factorial comb log ln exp sin cos tan min max); a_n is a plain variable, never a(n); no sets, tuples, subscripts.
- DEFINED FUNCTION: if a premise (here or earlier) DEFINES a function by a formula, e.g. f(x) = 3*x**3 - x + a, then your translated prop MUST NOT contain any f(...) term at all -- INLINE every f(<arg>) by substituting <arg> for the bound variable in the formula. Examples: with f(x) = 3*x**3 - x + a, translate the conclusion "f(2) = 22 + a" as 3*2**3 - 2 + a == 22 + a; translate "f(2) = 0" as 3*2**3 - 2 + a == 0; translate "f(3) = 3**2 + 1 = 10" (from f(x)=x**2+1) as 3**2 + 1 == 10. NEVER keep f(2) as a symbol and NEVER introduce a variable like f_2 or f2 for a defined function's value -- either loses the definition and the step cannot be verified. (An UNDEFINED function -- one only constrained by an equation it satisfies, with no closed formula -- has no formula to inline; translate its stated equalities literally as written.)
- Be faithful to the text AS WRITTEN: do not correct arithmetic, do not invent numbers. Every number the conclusion states must appear in prop doing real work - never satisfy this with tautologies (5 == 5) or by restating a given.
- Use the declared variables; introduce fresh lowercase names for new quantities and reuse them consistently across steps.
- When the conclusion names an aggregate or derived quantity (total, sum, remaining, difference, average) computed from quantities in this step's premises, write its defining relation explicitly as a conjunct: total_cost == house_cost + repair_cost and total_cost == 130000. The word's meaning is part of the conclusion.
- If a step concludes the final answer, write: answer == <value>.
- When correcting a previous output, you may re-output ONLY the STEP lines that were rejected; the other lines are kept automatically.

STEPS:
{steps}